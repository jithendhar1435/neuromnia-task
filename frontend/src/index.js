import React from 'react';
import ReactDOM from 'react-dom';
 // Optional if you have global CSS
import App from './App.js'; // Make sure this points to your App.js file

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
